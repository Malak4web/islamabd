# Project Orchestrator Plan

## Goal
Implement and verify all requirements specified in `ORIGINAL_REQUEST.md`:
1. R1: Home Page Hero & Overlay Light Mode Harmonization (`HeroSlider.vue`, `AboutSnippet.vue`, `ServicesPreview.vue`, `ProjectsPreview.vue`, `CtaBanner.vue`).
2. R2: High-Resolution Luxury Interior Asset Refresh (Hero slides 1-3, `HomeView.vue`, `ProjectSeeder.php`, `ServiceSeeder.php`).
3. R3: Icon & Decorative Element Harmonization (SVGs, Lucide icons, hover states, scroll indicators, lines).
4. R4: Build Verification & Test Integrity (`npm run build` and `php artisan test`).

## Workflow Strategy
1. **Phase 0: Parallel Codebase Survey**
   - Dispatch 3 parallel Explorer subagents (`teamwork_preview_explorer` / `teamwork_preview_spec_miner`) to analyze target components, database seeders, image assets, color utilities, build setup, and tests.
2. **Phase 1: Project Document Setup**
   - Create `c:\xampp\htdocs\islamabd\PROJECT.md` with Feature Inventory, Milestones, Code Layout, and Interface Contracts.
3. **Phase 2: Milestone Execution Loop**
   - M1: Home Page Hero & Overlay Light Mode Harmonization (R1)
   - M2: High-Resolution Luxury Interior Asset Refresh (R2)
   - M3: Icon & Decorative Element Harmonization (R3 + R1 initial scope across public/admin components)
   - M4: Build Verification & Test Integrity (R4)
4. **Phase 3: Verification & Reporting**
   - Verify build and tests via Reviewers/Challengers/Auditors.
   - Send completion report to Sentinel.
